name: Build

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # 최신 버전으로 유지

      - name: Set up Python
        uses: actions/setup-python@v4  # 최신 버전으로 유지
        with:
          python-version: '3.9'  # 필요에 맞는 Python 버전 설정

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # 필요에 맞는 패키지 설치

      - name: Build project
        run: |
          echo "Build step placeholder"
          # 실제 빌드 명령어를 추가합니다.

      - name: Run tests
        run: |
          echo "Test step placeholder"
          # 실제 테스트 명령어를 추가합니다.

      - name: Deploy
        run: |
          echo "Deploy step placeholder"
          # 실제 배포 명령어를 추가합니다.
